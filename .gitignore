__pycache__/
*.pyc
.env
*.log
*_master.jsonl
v1-agent-register-test-*.json
tmp-*.json
evidence.json
hermes-vps-*.json

# Runtime TrustScore cache
trustscore_cache.json
trustscore_cache.lock

# SAR-402 controlled demo generated runs (evidence + receipts)
reports/sar402-demo/runs/

# SAR-402 Path B local development credentials — never committed
.local-credentials/
dev/path-b-local/.env.local

# Durable authenticated-submission replay ledger (SQLite) — runtime state,
# never committed, same discipline as *_master.jsonl above.
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
